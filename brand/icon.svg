<!--
  Goodtype app icon. This is the one the application ships with.

  A page with one handwritten stroke above two lines of set type. Teal is only ever the
  handwritten part; ink-grey is only ever set type. Never tint the page outline or the set lines
  teal — that colour rule is the whole idea of the mark, and the product follows it too: teal
  means "your hand" wherever it appears.

  The tile is solid teal because an app icon has to carry its own silhouette. It used to be the
  app's own charcoal, which is within a couple of percent of the Windows taskbar, so the icon had
  no edge: what you saw was a stroke floating in the dark with no tile around it. The colour rule
  governs the mark's parts, not the ground they sit on — the hand is still the only teal thing on
  the page, and the page and its set lines are still paper.

  Clear space on all sides equals the page's corner radius.

  `cargo xtask icons` rasterises this into the PNG and ICO set Tauri needs. Edit the SVG,
  never the bitmaps.
-->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1024 1024" fill="none" role="img" aria-label="Goodtype">
  <rect width="1024" height="1024" rx="225.0" fill="#1497A6"/>
  <g transform="translate(20.48 181.17) scale(9.4523)">
    <rect x="6" y="6" width="92" height="58" rx="7" stroke="#FCFCFA" stroke-width="6"/>
    <path d="M18 30 C 24 18, 30 18, 34 28 C 37 35, 41 35, 45 26 C 48 19, 53 19, 57 24 C 60 28, 64 28, 67 23"
          stroke="#0C4A52" stroke-width="5.5" stroke-linecap="round" stroke-linejoin="round"/>
    <rect x="18" y="40" width="52" height="5.5" rx="2.75" fill="#FCFCFA"/>
    <rect x="18" y="48" width="38" height="5.5" rx="2.75" fill="#BFEAEF"/>
  </g>
</svg>
